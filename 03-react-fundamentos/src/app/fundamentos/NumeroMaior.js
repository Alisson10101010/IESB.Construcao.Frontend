

export default function NumeroMaior (props){

    console.log (props)

    return (
        <>
        <h2>Componente Numero Maior </h2>
        <p>O Numeros recebidos: (props.numB) - (props.numB) </p>
        <p>Propriedades Recebidas: (props.teste) </p>
        </>
    )
}